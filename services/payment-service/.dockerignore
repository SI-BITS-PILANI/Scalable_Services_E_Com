# Exclude local dependencies because they are installed inside the image.
node_modules

# Exclude local environment and git metadata from Docker build context.
.env
.env.*
.git
.gitignore

# Exclude test artifacts and editor settings.
coverage
.vscode
