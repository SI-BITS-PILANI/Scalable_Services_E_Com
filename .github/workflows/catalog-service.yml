# Catalog Service — CI
# Triggers: push/PR to services/catalog-service/**
# Runs: lint → unit tests → Docker build

name: Catalog Service

on:
  push:
    branches: [main, develop]
    paths:
      - "services/catalog-service/**"
  pull_request:
    branches: [main]
    paths:
      - "services/catalog-service/**"

defaults:
  run:
    working-directory: services/catalog-service

env:
  PYTHON_VERSION: "3.11"

jobs:
  # ---------------------------------------------------------
  # 1. Unit tests
  # ---------------------------------------------------------
  test:
    name: Unit Tests (Python 3.11)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"
          cache-dependency-path: services/catalog-service/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate gRPC stubs
        run: python -m grpc_tools.protoc -I./proto --python_out=. --grpc_python_out=. ./proto/catalog.proto

      - name: Run tests
        run: pytest -v --tb=short

  # ---------------------------------------------------------
  # 2. Build Docker image
  # ---------------------------------------------------------
  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image
        uses: docker/build-push-action@v5
        with:
          context: ./services/catalog-service
          push: false
          tags: catalog-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ---------------------------------------------------------
  # 3. Push image (on main only)
  # ---------------------------------------------------------
  push:
    name: Push Docker Image
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./services/catalog-service
          push: true
          tags: |
            ghcr.io/${{ lower(github.repository) }}/catalog-service:${{ github.sha }}
            ghcr.io/${{ lower(github.repository) }}/catalog-service:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
